
const prefix = '@welcome-page';
export const PREFIX = new RegExp(prefix, 'i');
export const SHOW = `${prefix}/SHOW`;
export const CLEAR = `${prefix}/CLEAR`;

export default {
    PREFIX,
    CLEAR,
    SHOW,
};
